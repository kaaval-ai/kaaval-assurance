"""kaaval-assurance: the Kaaval inference assurance plane.

Layer 1: deterministic contract verification (built).
Layer 2: per-category EWMA trend tracking (seam only in Jul 5 scope).
Layer 3: sampled adversarial audit (schema seam only in Jul 5 scope).
"""

__version__ = "0.1.0"
